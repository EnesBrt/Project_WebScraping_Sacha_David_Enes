# Celsius => Fahrenheit
def CELtoFAR(temperature):
    temperature = ((temperature * 9/5) + 32)
    return round(temperature)

# Fahrenheit => Celsius
def FARtoCEl(temperature):
    temperature = ((32 - temperature) * 5/9)
    return round(temperature)
